# External ports exposed by each service on the host machine.
# Change these values if any port is already in use on your system.
PRODUCER_PORT=7001
PRODUCER_LOCATION=/kafka/producer
CONSUMER_PORT=7002
CONSUMER_LOCATION=/kafka/worker
DLQ_PORT=7003
DLQ_LOCATION=/kafka/dlq
